{
  "name": "monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "devh": "bun run --filter '*' devh",
    "dev-be": "bun run --filter 'backend' dev",
    "dev:pg": "bun run --filter '*' dev:pg",
    "build": "bun run --filter '*' build",
    "preview": "bun run --filter '*' preview"
  }
}